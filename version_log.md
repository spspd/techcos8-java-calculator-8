
각 버전별 추가 고민 한점 
1. 문자열 입출력 확인
2. ,를 구분자로 가지는 문자열을 주어진 라이브러리를 사용해서 처리후 계산 
   1. 이론적으로 간단하지만 문자열을 끊어야함 
   2. 문자열 A 가 문자열 a , 연산자 , b이렇게 세개로 끊어짐 ( 미리 고려안했음
   3. 막 구현하면서 할지 미리 논리적으로 구현하고 만들지 고민 
   4. sb 
   5. 초기버전 안올려서 리팩토링 부분 삭제되었음
      1. 초기버전은 추가 함수를 작성하지 않고 , 를 확인하는 방식으로 구현 
      2. :이 추가된 상태에서 commit 하였다

3. :를 추가 및 구분자가 긴 문자열 이여도 상관없도록 구현되었는지 확인
   1. 긴 문자열을 위한 리팩토링 작업 시작
   2. 긴 문자열 확인
      1. string의 equals를 사용하거나 앞에서부터 "동일한 문자열인지" 확인
      2. 고민 하게 되는 문제는 한쪽은 stream으로 사용하고 한쪽은 문자열 이라는 것 
         1. 이후 stream으로 보고 제작시 동일 문자 시작 길이 다른 경우의 문제 발생할듯
         2. "재사용 가능성" 과 "프로그램이 규모가 적절하게 커질 수도 있다"를 생각한다면 사실 ast트리를 만들어서 앞에서 부터 stream으로 비교하는 방식으로 제작하는게 모든 경우에 대응 할수 있어서 가장 좋아보인다.
            1. 하지만 이번의 경우 늦은 시작으로 패스  
      3. 문자열에 숫자가 있을 경우의 문제점때문에
         1. 어떤 지점부터 앞에서부터 일정 길이를 확인해서 대체 하는 방식이 가장 편하다
         2. 이걸 우리는 파싱이라고 하기로 했어요..
         3. 그래서 이시점에 어떻게 리팩토링을 할지를 결정해야한다.

   3. 특이사항시 오류나는지 확인
4. // \n 문법을 사용해서 커스텀 입력자 추가 
   1. 오류 검증
